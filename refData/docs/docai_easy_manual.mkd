沒問題！這是一個非常好的調整方向。將焦點集中在 **「Skill-Based (技能導向)」** 架構上，能讓 DocAI 從單純的「檔案搜尋工具」昇華為「專業知識庫建構平台」。這對一般使用者來說更具吸引力，也更符合「個人圖書館員」的願景。

以下是排除舊版 File-based 功能後，專為 **Skill-Based RAG** 重新梳理的專案簡介、使用手冊與未來展望。

---

# DocAI：您的個人專屬領域知識專家

## 1. DocAI 簡介 (Introduction)

**DocAI** 是一個運行在本地端的智慧型知識庫系統。與一般只能搜尋檔案的工具不同，DocAI 採用了先進的 **「技能導向 (Skill-Based)」** 架構。

它的核心概念是將零散的文件轉化為具備特定專業能力的「技能 (Skills)」。你可以將 DocAI 想像成一位可以無限學習的專家，你「教」給它什麼文件，它就成為該領域的權威。

### 核心亮點

* **自訂知識領域 (Curated Knowledge Domains)**：你可以創建特定的「技能」，例如「人資法規專家」、「Python 寫作助手」或「專案 A 規格說明」，並將相關的多份文件整合到該技能中。
* **絕對隱私與本地運行**：支援 Ollama 本地模型 (如 deepseek-r1, llama3)，所有資料處理都在你的電腦上完成，無需擔心機密文件外流。
* **輕量化與高精確度**：使用 **FAISS** 向量索引技術與 **SQLite** 管理架構，專為精確的領域問答優化，確保回答內容聚焦且專業。

---

## 2. 一般使用者簡易手冊 (User Manual)

本手冊將引導你如何建立並諮詢你的專屬「AI 專家」。

### 準備工作

1. **啟動系統**：在專案資料夾中執行 `python main.py`，並打開瀏覽器前往 `http://localhost:8000`。
2. *(初次使用)* 確保已安裝 Python 套件 (`pip install -r requirements.txt`) 並確認 Ollama 已在背景執行。

### 核心操作流程：打造你的 AI 專家

#### 步驟一：創建「技能 (Skill)」

不要只是上傳檔案，我們先定義「專長」。

1. 進入 **「技能管理 (Skill Management)」** 介面。
2. 點擊「新增技能」，為它取個名字。
* *範例*：命名為 **「公司請假規範」**。



#### 步驟二：賦予知識 (Ingest Documents)

1. 點擊剛建立的技能。
2. 上傳相關的 PDF 文件（例如：員工手冊.pdf、請假流程圖.pdf）。
3. 系統會自動閱讀這些文件，將其吸收為該技能的專屬知識庫。

#### 步驟三：諮詢專家 (Consulting)

1. 回到對話介面，**選擇你剛建立的技能**（例如「公司請假規範」）。
2. 開始提問：
* *問*：「如果是病假，需要提供什麼證明？」
* *答*：DocAI 不會搜尋全世界，它只會根據你教它的「員工手冊」內容，精準回答你需要的證明文件與流程。



---

## 3. 未來開發方向及應用 (Future Directions & Applications)

專注於 Skill-Based 架構後，DocAI 擁有極大的擴展潛力，從「個人工具」轉向「知識管理平台」。

### A. 可攜式知識包 (Portable Skill Packs)

* **概念**：目前的技能資料儲存在獨立的 SQLite 與 FAISS 索引中。
* **開發方向**：開發「匯出/匯入」功能。
* **應用場景**：
* 資深員工整理好一份「新人入職懶人包 (Skill)」，直接匯出成一個檔案傳給新進員工。新人匯入後，立刻擁有一個能回答所有入職問題的 AI 助手，無需重新訓練。



### B. 專家角色扮演 (Persona Configuration)

* **概念**：結合 Prompt Engineering，為不同的 Skill 設定不同的人格。
* **開發方向**：在創建 Skill 時，允許使用者設定「系統提示詞 (System Prompt)」。
* **應用場景**：
* **「嚴格的代碼審查員」Skill**：讀取程式碼規範文件，用嚴謹、批判的語氣回答。
* **「溫柔的客服導師」Skill**：讀取客服SOP，用鼓勵、引導的語氣教導新進客服如何應對奧客。



### C. 技能與技能的協作 (Multi-Skill Orchestration)

* **概念**：目前是針對單一 Skill 提問。
* **開發方向**：開發一個「總管 AI (Manager Agent)」。
* **應用場景**：
* 使用者問：「我要申請出差，且需要預支旅費，該怎麼做？」
* 總管 AI 自動判斷，分別去查詢 **「出差規範 Skill」** 和 **「財務報銷 Skill」**，最後將兩邊的資訊整合，給出一個完整的跨部門解答。



### D. 垂直領域的教育訓練

* **應用場景**：學校或補習班老師可以建立特定課程的 Skill（如「歷史-二戰篇」），上傳所有講義。學生下課後可以針對這個 Skill 進行複習與問答，AI 就像助教一樣，永遠不會累且隨時在線。

---
