/sc:brainstorm  ```
1. That is why we need users table, the followings are our next tasks, the first is to create user management page, we can put in the third tab.
2. According to the users table, some field need user to key in data. as a result on the users management page should have a [new user] button, when user click the button, add new user's modal window shows, it contains the necessary fields for users table to insert new row.
3. 
```